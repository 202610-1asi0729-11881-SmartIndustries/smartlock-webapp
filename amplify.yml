version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build --configuration=production
  artifacts:
    baseDirectory: dist/smartlock-webapp/browser
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
